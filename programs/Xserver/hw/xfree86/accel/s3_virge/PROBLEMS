READ_MASK and WRITE_MASK don't exist anymore?

WRITE_MASK could be simulated by setting up a pattern will all pixels using
the same mask bitmap but this would need to set 64bytes for 8bpp before
being able to use the write_mask (and needs another set of s3alu[] 
using ROPs with "... and pattern".

how to simulate READ_MASKs where needed ?



LINES
always drawn bottom to top (decreasing Y), how do we make sure the same
pixels are set as for normal X11 vector generator ?


LINE endpoints
how to setup end0/end1 for connected line segments?
if they really describe the X coordinate of the first/last pixel 
(and not the MAJOR coordinate) this isn't useful for lines 
with abs(slope) > 1


24bpp support needs to setup the STREAMS processor correctly


CLIPPING:
when drawing a font glyph (transfering font bitmap from CPU via IMG_TRANS) 
outside the clipping area the GE doesn't draw anything and thus don't
expect any data.  writing to IMG_TRANS anyway will fill the FIFO
and finally lock up the PC :-(
so for now hardware clipping is disabled in s3text.c...


