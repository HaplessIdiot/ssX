
ITEMS THAT SHOULD BE COMPLETED BEFORE FIRST PUBLIC RELEASE:
----------------------------------------------------------------------

Finish writing the help text

Write documentation (modify the QuickStart guide)

Clean up temp files (but leave final temp config file if they did
   a reconfig without root privileges)

fix stack handling: -ls option doesn't work on ISC

When reading symlinks, resolve relative paths

Check for symlink loops

Update man page


ITEMS THAT WOULD BE NICE TO HAVE BEFORE FIRST PUBLIC RELEASE:
----------------------------------------------------------------------

When using the SVGA server, further restrict the list of available
   RAMDACs, clock chips, and READMEs once the chipset has been selected.

Add keybindings to enter and/or space in listboxes to match the
   button-up binding

Sort items in listboxes

Check that mouse device link can only be changed by root

Change behaviour (with a 'u' for David :-) of "Probed" buttons as per
Harald's request:

> the functionality of "RAMDAC max speed" and "Video RAM" is somewhat different.
> while it's possible to just select the memory size if wanted/needed I can't
> change the ramdac speed without "unlocking" the slider first (not too intuitiv
e).
>
> would it be possible to change the text of those "Probed" buttons if not in
> probed state (either show "User defined" or the actual value).

Reset clocks when a programmable clock chip is selected or when
  changing servers or whenever else would be appropriate

More reasonable default Device and Monitor identifiers



(POSSIBLE) ITEMS FOR SOME TIME IN THE FUTURE:
----------------------------------------------------------------------

Add configuration of related things that can be set with xset such as
   keyboard repeat rate, mouse acceleration, screen saver settings,
   etc. ???  - this probably should be a separate client

Check for proper permissions of various files ???

If files are missing, go ftp them for the user ???

More fine-grained control of what is read from the existing XF86Config

Add support for XInput

Add support for Modules

Add ability to select modes and their order


RELATED CHANGES TO OTHER PARTS OF THE X SOURCE TREE:
----------------------------------------------------------------------

Update VidMode extension to know about the Hskew parameter

Add ability to dynamically add & remove video modes ???

Alter XFree86-Misc extension to automatically enable allowMouseOpenFail
   whenever a mouse change is requested (we don't want a failed
   attempt to reopen the device to cause the server to die)

Alter xf86SetMouseSpeed to not cause a fatal error if it can't write
   to the mouse device, if allowMouseOpenFail is enabled

Update os-support/README.OS-lib with info regarding allowMouseOpenFail

Update XF86Conf.{man,cpp} to match latest XKB changes

