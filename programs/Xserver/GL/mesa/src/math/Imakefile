XCOMM $XFree86: xc/programs/Xserver/GL/mesa/src/math/Imakefile,v 1.2 2002/02/25 00:45:44 dawes Exp $

#define IHaveModules
#include <Server.tmpl>

#define NeedAllMesaSrc
#define NeedToLinkMesaSrc
#define MesaMathBuildDir /**/
#include "../../../../../../lib/GL/mesa/src/math/Imakefile.inc"

         SRCS = $(MESA_MATH_SRCS)
         OBJS = $(MESA_MATH_OBJS)

     INCLUDES = -I$(MESASRCDIR)/src \
		-I$(MESASRCDIR)/src/math \
		-I$(MESASRCDIR)/include \
                -I$(SERVERSRC)/include -I$(XINCLUDESRC) \
                -I../../../include -I../../../glx \
                -I$(LIBSRC)/GL/include \
                -I$(XF86SRC) -I$(INCLUDESRC)

      DEFINES = $(GLX_DEFINES) /*-DUSE_X86_ASM*/ /*-DUSE_SPARC_ASM*/

#if DoLoadableServer
ModuleObjectRule()
#else
NormalLibraryObjectRule()
#endif

SubdirLibraryRule($(OBJS))

LintLibraryTarget(dri,$(SRCS))
NormalLintTarget($(SRCS))

DependTarget()
