#!/bin/sh

# Generate gloffsets.h, glapitable.h and glapi_x86.S files from
# the gl.spec file (found in SGI's si).

set -v

./gltable.py > ../src/glapitable.h

./gloffsets.py > ../src/glapioffsets.h

./glx86asm.py > ../src/X86/glapi_x86.S

