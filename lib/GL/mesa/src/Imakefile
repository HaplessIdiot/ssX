XCOMM $XFree86: xc/lib/GL/mesa/src/Imakefile,v 1.22 2001/03/21 16:14:20 dawes Exp $

#include <Threads.tmpl>

/* Only need SharedLib build unless OSMesa lib is built */
#if BuildXF86DRI && !GlxUseSGISI && \
        (!GlxBuiltInMesa || !defined(GlxDriverUsesMesa))
#define DoNormalLib NormalLibGlx
#define DoSharedLib SharedLibGlx
#define DoExtraLib SharedLibGlx
#define DoDebugLib DebugLibGlx
#define DoProfileLib ProfileLibGlx
#else
#define DoSharedLib YES
#define DoNormalLib NO
#define DoExtraLib NO
#define DoDebugLib NO
#define DoProfileLib NO
#endif

#if Malloc0ReturnsNull
ALLOC_DEFINES = -DMALLOC_0_RETURNS_NULL
#endif


#ifdef i386Architecture
#define MesaX86BuildDir /**/
#include "X86/Imakefile.inc"
#endif
#define MesaBuildDir /**/
#define NeedToLinkMesaSrc
#include "Imakefile.inc"

#ifdef UseCompaqMathLibrary
      MATHDEF = -DCCPML
#endif

      DEFINES = $(ALLOC_DEFINES) GlxDefines $(TDFX_DEFS) \
		$(MESA_ASM_DEFS) $(MATHDEF)
     INCLUDES = -I$(INCLUDESRC) -I$(XINCLUDESRC) -I$(EXTINCSRC) \
		-I$(MESASRCDIR)/src -I$(GLXLIBSRC)/dri
         SRCS = $(COREMESASRCS)
         OBJS = $(COREMESAOBJS)

#include <Library.tmpl>

LibraryObjectRule()

SubdirLibraryRule($(OBJS))
NormalLintTarget($(SRCS))

#ifdef i386Architecture

#define IHaveSubdirs
#define PassCDebugFlags

SUBDIRS = X86

MakeSubdirs($(SUBDIRS))
DependSubdirs($(SUBDIRS))

#endif

DependTarget()
